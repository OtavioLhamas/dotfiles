# Auto-generated by chezmoi — do not edit manually
all:
  hosts:
    localhost:
      ansible_connection: local
  children:
{{ range $cat := .categories }}
    {{ $cat }}:
      hosts:
        localhost:
{{ end }}
    {{ .form_factor }}:
      hosts:
        localhost:

    {{ .desktop_environment }}:
      hosts:
        localhost:
