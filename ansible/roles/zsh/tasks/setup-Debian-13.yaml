---
- name: Install zsh
  ansible.builtin.apt:
    update_cache: true
    name: "{{ zsh_package }}{% if zsh_version != 'latest' %}={{ zsh_version }}{% endif %}"
    state: present
    allow_downgrade: "{{ zsh_version != 'latest' }}"
  become: true
