# Auto-generated by chezmoi — do not edit manually
all:
  hosts:
    localhost:
      ansible_connection: local
  children:

    work:
      hosts:
        localhost:

    personal:
      hosts:
        localhost:

    gaming:
      hosts:
        localhost:

    multimedia:
      hosts:
        localhost:

    development:
      hosts:
        localhost:

    desktop:
      hosts:
        localhost:
    gnome:
      hosts:
        localhost:
